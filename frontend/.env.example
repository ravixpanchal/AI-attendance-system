# Leave empty to use the Vite proxy (see vite.config.ts target port).
# Local dev default in .env uses port 8765 (avoids Windows socket errors on some PCs).
# VITE_API_URL=http://127.0.0.1:8765
# Production example:
# VITE_API_URL=https://your-api.onrender.com
